# TASK-03: SQLite DB 기반 매수/매도 거래 내역 및 데이터 리프레시 고도화 (_FE_R3)

- **작성일:** 2026. 05. 17
- **작성자:** 기획자 MOON(무니)
- **승인자:** 의사결정권자 SUN(써니)
- **담당자:** EARTH-FE(어띠페)

---

## 📌 [COMMON] 공통 요구사항 (UI 동기화 고도화)
- 거래 내역 등록 후 메인 대시보드의 총자산 및 보유 종목 리스트가 실시간으로 일치하지 않는 현상을 방지하기 위해 **상태 동기화 로직(Data Refresh)**을 강화한다.

## 🌍 [FE_TASK] 프런트엔드 상세 구현 지침
- **데이터 흐름 제어 (Refetch 공수):**
  - 매매 등록 폼에서 [등록 완료]를 처리하는 `POST /api/transactions/add`가 성공 응답(`status: success`)을 반환하는 즉시, 상위 대시보드 컴포넌트의 `fetchAccountData()` 함수를 재트리거하라.
  - 이를 통해 [탭 1: 보유 자산 상세]의 `DataTable`과 [탭 2: 매매 내역 히스토리]의 `DataTable`이 동시에 최신 SQLite DB 상태를 바라보도록 화면 리프레시 로직을 결합하라.

## 🏁 완료 조건
1. 매매 거래를 등록하고 모달이 닫히는 순간 화면 깜빡임 없이 상하단 데이터 테이블이 실시간 동기화되는가?
2. Lint 및 Prettier 포맷터 규칙을 통과했는가?
